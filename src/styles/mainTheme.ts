import { DefaultTheme } from "styled-components";

const mainTheme: DefaultTheme = {
  borderRadius: "8px",
  typography: {
    fontFamily: "Rubik",
    sansSerif: "DM Sans , sans-serif",
  },
};

export { mainTheme };
